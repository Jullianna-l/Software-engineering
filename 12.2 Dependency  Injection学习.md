## Dependency  Injection-A Practical Introduction学习

### 1.DI 的核心价值：降低耦合度，提升可维护性

（1）通过将类对依赖项的控制反转为外部传入（而非内部创建），DI 实现了控制反转（IoC）的目标

（2）将组件间的依赖关系转移为接口依赖，而非直接依赖具体实现，从而实现松耦合设计。

（3）松耦合使得代码更具可扩展性（Extensibility）、可测试性（Testability）和可维护性（Maintainability），并支持运行时依赖替换（Late Binding）。



### 2.紧耦合的代码限制了单元测试与组件替换

（1）报告中的示例项目展示了传统“new”对象创建方式如何导致UI层与服务层之间不必要的耦合，影响未来扩展与测试。

（2）若添加新的数据访问方式（如 SQL、CSV、缓存），必须修改并重编译视图模型或上层模块，违背了开闭原则



### 3.DI 支持多种实现方式

三种常见注入方式：构造函数注入（Constructor Injection）、属性注入（Property Injection）、和方法注入（Method Injection），其中构造函数注入为首选。

（1）构造函数注入（Constructor Injection）：最推荐方式，能显式表达依赖项需求。

（2）属性注入（Property Injection）：适用于依赖项具有合理默认值的场景。

（3）方法注入（Method Injection）：用于仅在特定方法中临时使用的依赖。

| 注入方式         | 特点                               | 适用场景                     |
| ---------------- | ---------------------------------- | ---------------------------- |
| **构造函数注入** | 显式、强制依赖，编译期可控         | 主要依赖项                   |
| 属性注入         | 可选依赖，支持默认值，灵活但易忽视 | 缓存、日志、配置等可选依赖   |
| 方法注入         | 仅在调用某些方法时临时使用         | 特殊场景（如工厂或执行策略） |



### 4.依赖抽象接口以实现可扩展结构

（1）使用如 IPersonRepository 的接口可以实现多个具体仓储（CSV、数据库、缓存等）的无缝替换。

（2）体现了开闭原则（OCP）与依赖倒置原则（DIP）：高层模块不再依赖低层实现，而依赖其抽象。



### 5. DI 容器简化依赖管理与生命周期控制

使用 Unity 与 Ninject 等流行容器框架：

（1）自动解析依赖关系

（2）管理生命周期（Transient、Singleton 等）

（3）支持基于 XML 的配置实现运行时依赖替换（Late Binding）

容器充当 Composition Root，集中管理对象图构建与注入逻辑。



### 6.装饰器模式 + DI：增强行为而无需修改原类

通过 DI 构建了缓存型仓库类（CachingRepository），使用装饰器模式将其包装在其他仓库外层以增强功能，遵循开放封闭原则，体现装饰器模式的价值。



### 7.DI 显著提升了单元测试的效率与质量

（1）通过注入 Mock 对象，测试可聚焦于实际被测单元，避免外部依赖干扰。

（2）使用 Moq 框架轻松实现接口替身，验证 ViewModel 和 Repository 的行为。

（3）实现真正的单元测试隔离：仅测试目标模块，而非其依赖链



### 8.依赖注入使用

（1）**基本原则**：类不应该自己创建依赖，而应该**通过外部传入依赖对象**（也就是“注入”），从而达到“松耦合”目的。

（2）实现：为依赖定义接口（抽象）->实现接口的具体类->在使用类中注入接口依赖(构造函数注入最常实现)->在组合根（Composition Root）中组装依赖关系->使用 DI 容器自动管理依赖



### 总结

（1）依赖注入是一种思维方式，而不仅是技术工具。它推动开发者以接口为中心进行设计，并将控制权下放到外部。

（2）避免在模块内部直接 new 实例 是实现解耦的第一步，推荐总是依赖接口 + 构造注入。

（3）DI 容器不是必须，但在大型项目中必不可少。它能大幅简化对象图构建，尤其在需要复杂生命周期管理和运行时切换依赖时。

（4）正确使用依赖注入，能够自然落实 S.O.L.I.D 原则，从而构建出高内聚、低耦合、易测试、可演化的现代软件系统。